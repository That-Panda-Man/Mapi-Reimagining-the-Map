{
  "name": "mapi",
  "version": "1.0.0",
  "description": "Mapi App",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "build": "cd frontend && npm install && npm run build",
    "install:all": "npm install && cd frontend && npm install"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.90.1"
  },
  "devDependencies": {
    "netlify-cli": "^23.13.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
